# Listener Utilities

Utility scripts for the Canarytoken alerts webhook listener.

## Scripts

- `start-canary-listener-ingester.sh` -  Starts the listener 
- `listener9000.py` - If run along with `mongoDB/save-log-cowrie.py` it can be used for testing