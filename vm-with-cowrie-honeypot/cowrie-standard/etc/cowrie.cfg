[honeypot]
# Hostname displayed in shell prompt
hostname = unibo-lab4

# Timezone for logging (Italy)
timezone = Europe/Rome

# Log directory (separate for vanilla instance)
log_path = var/log/cowrie_vanilla

# State directory (tty logs, keys, downloads)
state_path = var/lib/cowrie_vanilla

# Data directory (fs.pickle lives here)
data_path = src/cowrie/data

# Auth: the attacker randomly tries from 2 to 10 
auth_class = AuthRandom
auth_class_parameters = 2, 10, 50

[shell]
# Points to pickle file
filesystem = src/cowrie/data/fs.pickle

# Architecture and OS details (customize as needed)
arch = x86_64
kernel_version = 5.4.0-150-generic
operating_system = GNU/Linux
env = {"LANG": "it_IT.UTF-8"}

[ssh]
# Instance 1 listens on port 2223 (internal only)
listen_endpoints = tcp:2223:interface=127.0.0.1

[output_jsonlog]
enabled = true
logfile = ${honeypot:log_path}/cowrie_vanilla.json

[output_textlog]
enabled = true
logfile = ${honeypot:log_path}/cowrie_vanilla.log
